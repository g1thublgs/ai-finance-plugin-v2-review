const QWEN35_OCR_GUIDANCE = `
【qwen35-35b-a3b 模型专用约束】
1. qwen35-35b-a3b 是视觉语言 MoE 模型，接口层已按 OCR 场景传入 enable_thinking=false。你可以在内部完成版面定位和字段定位，但最终只输出 JSON，不输出推理、解释、置信度、Markdown、代码块或 <think>。
2. 本任务是“结构化 OCR”，不是问答、摘要或财务判断。所有字段值必须来自当前图片可见文字；不得依据文件名、历史页、相邻页、常识或示例值补全。
3. 先在内部按“单据类型证据 -> 字段区域 -> 表格行 -> JSON schema”四步处理，但不要输出这四步过程。
4. 输出必须是严格 JSON 对象。单图固定为 {"data":[...]} 或 {"data":[]}；多图固定为 {"files":[{"fileName":"...","data":[...]}]}。不要在 JSON 外输出任何字符。
5. 字段名必须完全使用模板中的英文 key。不要新增字段、不要改字段名、不要输出中文 key。
6. 看不清字段填空字符串 ""；数组字段看不到明细填 []。不要输出 null、undefined、未知、未识别、无、N/A。
7. 只要能确认单据类型并看到至少 1 个关键字段，就返回对应 recognizeType，其余字段留空；只有完全看不到业务关键字段、只有页码/水印/二维码/空白区域时，才返回 {"data":[]}。
8. 示例只用于约束字段格式，不得复制示例中的人名、金额、票号、地点到真实输出。
9. 如果页面包含发票号码、数电票号码、电子发票、普通发票、增值税发票、开票日期、价税合计、本次实收、购买方、销售方中的任一发票证据，并能确认是发票，recognizeType 必须是 normalInvoice，不得返回 other。
10. 金额字段只输出数字字符串，不带人民币符号、逗号、单位；日期尽量输出 YYYY-MM-DD，带时间尽量输出 YYYY-MM-DD HH:mm。
11. 表格必须逐行抽取：发票项目明细、住宿清单、行程明细、人员名单、菜单明细都不要合并成一条汇总行。
12. 一张图片内如果有多张同类票据（例如多张出租车小票、多个付款截图拼图），必须按票据边框/标题/票号逐张拆成多条 data，不要只返回第一张或汇总金额。
13. 火车票、飞机票等实名票据中，姓名字段必须来自“姓名/旅客/乘车人/乘机人”可见姓名区域，不要把身份证号、证件号、票号当成人名。
14. 多图/多页识别时，每张图片只能识别本图内容，严禁把上一页或下一页数据复制到当前页；空白页必须保留对应 fileName 并返回 data:[]。
`.trim();

function buildQwen35BatchGuidance(files = []) {
    const list = files.map((file, index) => `${index + 1}. ${file.fileName || file.name || `page_${index + 1}`}`).join('\n');
    return `
【qwen35-35b-a3b 多图/多页输出要求】
本次会同时发送多张图片，请按图片顺序返回：
{"files":[{"fileName":"图片文件名","data":[...]}]}
files 长度必须等于输入图片数量；fileName 必须完全照抄下方图片列表。
每张图片只能识别自己的内容，严禁把上一张或下一张图片的数据复制给空白页。
空白、无票据、低证据图片必须输出 "data":[]，不要省略该 fileName。

图片列表：
${list}
`.trim();
}

module.exports = {
    QWEN35_OCR_GUIDANCE,
    buildQwen35BatchGuidance,
};
